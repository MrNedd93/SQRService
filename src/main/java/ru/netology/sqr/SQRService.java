package ru.netology.sqr;

public class SQRService {
    public int calculate(int limMin;int limMax) {
        for (int i = 10; i <= 99; i++) {
            if (i * i >= limMin && i * i <= limMax){
                int counter = 0;
        }
    }
}
}